{
  "name": "langchain-intro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_OPTIONS=--no-experimental-detect-module node --env-file .env --inspect --watch src/index.ts",
    "test": "NODE_OPTIONS=--no-experimental-detect-module node --env-file .env --test ./__tests__/**/*.test.ts",
    "test:dev": "NODE_OPTIONS=--no-experimental-detect-module node --env-file .env --inspect --watch --test ./__tests__/**/*.test.ts",
    "langgraph:new": "npx @langchain/langgraph-cli@latest new",
    "langgraph:server": "npx @langchain/langgraph-cli@latest dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@langchain/core": "^1.1.19",
    "@types/node": "^24.12.2",
    "fastify": "^5.7.4",
    "langchain": "^1.2.17",
    "zod": "^3.25.76"
  }
}
